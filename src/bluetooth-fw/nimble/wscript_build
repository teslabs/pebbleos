excludes = []
if not bld.env.CONFIG_GH3X2X_TUNING_SERVICE_ENABLED:
    excludes.extend(['gh3x2x_tuning_service.c'])

driver_source = bld.path.ant_glob(['*.c'], excl=excludes)

bld.pbl_library(driver_source, name='bt_driver')

# vim:filetype=python
