# SPDX-FileCopyrightText: 2026 Core Devices LLC
# SPDX-License-Identifier: Apache-2.0

sources = []
use = []

if bld.env.CONFIG_HRM_GH3X2X:
    sources.append('gh3x2x.c')
    if bld.env.VARIANT != 'prf' or bld.env.CONFIG_MFG:
        bld.env.DEFINES.append('HRM_USE_GH3X2X')
        use.append('gh3x2x')
elif bld.env.CONFIG_HRM_STUB:
    sources.append('stub.c')

bld.pbl_library(sources, use=use)

# vim:filetype=python
