# Recovery (PRF) firmware Kconfig overrides.
# Applied on top of prj.conf when building with --variant=prf.

# PRF services.
CONFIG_SERVICE_IDLE_WATCHDOG=y

# Recovery firmware identity.
CONFIG_RECOVERY_FW=y
