# SPDX-FileCopyrightText: 2026 Core Devices LLC
# SPDX-License-Identifier: Apache-2.0

[pytest]
pythonpath = .
testpaths = system ui notifications power prf
addopts = --strict-markers -p no:cacheprovider
log_level = INFO
# xunit2 drops the properties tests record (e.g. current measurements).
junit_family = xunit1
