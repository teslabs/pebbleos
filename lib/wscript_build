# SPDX-FileCopyrightText: 2026 Core Devices LLC
# SPDX-License-Identifier: Apache-2.0

# Only the libraries the unit tests link against; the firmware build is
# CMake's (see lib/CMakeLists.txt).
bld.recurse('util')
bld.recurse('btutil')

# vim:filetype=python
