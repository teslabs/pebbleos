import tools.waf

bld.pbl_library(bld.path.ant_glob('**/*.c'),
                name='libutil',
                kind='stlib',
                includes=['.'],
                env=bld.env.derive())

# vim:filetype=python
