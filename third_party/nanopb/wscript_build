nanopb_sources = [
    'nanopb/pb_common.c',
	'nanopb/pb_encode.c',
	'nanopb/pb_decode.c',
]

nanopb_includes = [
    'nanopb',
]

bld.pbl_include_directories(*nanopb_includes)
bld(name='nanopb_includes', use=['pbl_interface'])

bld.stlib(source=nanopb_sources,
          includes=nanopb_includes,
          export_includes=nanopb_includes,
          target='nanopb')
